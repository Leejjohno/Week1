//  -----------------------------Activity 1 (Array with Loop)-------------------------

let favFilm = [
    "Blade Runner",
    "Terminator",
    "Terminator 2",
]
    favFilm.push("Heat", "Aliens")

    for (let i = 0; i < favFilm.length; i++) {
        console.log(favFilm[i])
    }
